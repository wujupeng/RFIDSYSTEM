# RFID 决策系统 v2.3 - 使用说明

## 系统概述

这是一个基于规则的决策系统，将 RFID 数据转化为可执行的决策。

---

## 核心组件

### 1. 预测层 (Predictor)
- 文件: `server/analytics/predictor/asset_predictor.h/cpp`
- 功能: 预测资产状态和风险
- 输出: RiskScore 结构
  - missing_risk: 丢失风险 (0-1)
  - inactivity_risk: 闲置风险 (0-1)
  - abnormal_risk: 异常风险 (0-1)

### 2. 评分层 (Scoring)
- 文件: `server/analytics/scoring/asset_score.h/cpp`
- 功能: 计算资产健康度和风险等级
- 输出: AssetScore 结构
  - health_score: 健康度 (0-100)
  - risk_level: 风险等级 (LOW/MEDIUM/HIGH/CRITICAL)

### 3. 决策引擎 (Decision Engine)
- 文件: `server/analytics/decision/decision_engine.h/cpp`
- 功能: 基于规则生成决策
- 支持的决策类型:
  - INSPECT: 立即巡检
  - CHECK_USAGE: 检查使用
  - SECURITY_ALERT: 安全告警
  - RELOCATE: 重新分配
  - MAINTENANCE: 维护检查
  - NO_ACTION: 无需行动

### 4. 行动生成器 (Action Generator)
- 文件: `server/analytics/decision/action_generator.h/cpp`
- 功能: 生成人类可读的行动报告

### 5. 决策沙盒 (Decision Sandbox)
- 文件: `tools/decision_sandbox.cpp`
- 功能: 离线验证决策系统

---

## 关键优化

### 修复1: 只对活跃设备判断丢失
```cpp
bool isActive = dailyAvgScans > active_asset_threshold_;
if (!isActive) {
    // 不触发丢失告警
}
```
- 避免低频设备误报丢失

### 修复2: 备用设备不触发闲置
```cpp
if (isBackup) {
    // 备用设备闲置为正常状态
}
```

### 修复3: 使用去抖动后的移动次数
- 参数: `moveCount24h` 应为去抖动后的真实移动次数

---

## 使用方式

### 基本流程
```cpp
#include "server/analytics/predictor/asset_predictor.h"
#include "server/analytics/scoring/asset_score.h"
#include "server/analytics/decision/decision_engine.h"

using namespace analytics;

// 1. 获取预测
auto prediction = AssetPredictor::instance().predictAssetStatus(
    asset_id,
    hours_since_last_seen,
    daily_avg_scans,
    weekly_avg_scans,
    abnormal_events,
    location_changes_24h
);

// 2. 计算评分
auto score = AssetScoring::instance().calculateAssetScore(
    prediction.risk_score.missing_risk,
    prediction.risk_score.inactivity_risk,
    prediction.risk_score.abnormal_risk,
    daily_avg_scans,
    repair_count
);

// 3. 生成决策
auto decision = DecisionEngine::instance().makeDecision(
    asset_id,
    asset_name,
    current_location,
    prediction.risk_score,
    score,
    in_illegal_location,    // 非法区域
    move_count_24h,         // 去抖动后的移动次数
    missing_hours,          // 未扫描小时数
    daily_avg_scans,        // 日常扫描频率
    asset_type,             // 资产类型
    is_backup               // 是否备用设备
);
```

### Shadow Mode (影子模式)
先运行 3-7 天，只记录不执行，验证决策准确性:
```cpp
// 只记录到数据库，不实际执行
DecisionRepository::save(decision);
```

---

## 数据库迁移

执行 `scripts/migration_v2.3.sql` 创建新表:
- `asset_predictions`: 预测记录
- `asset_scores`: 评分记录
- `decisions`: 决策记录
- `auto_tasks`: 自动任务（预留）

---

## 决策沙盒验证

### 编译
```bash
mkdir build && cd build
cmake ..
make decision_sandbox
```

### 运行
```bash
./tools/decision_sandbox
```

### 输出示例
```
========================================
    决策系统验证报告 - v2.3
========================================

Asset #1: Laptop-001
  Type: laptop
  Last seen: 72 hours ago
  Avg scans: 8.5/day
  Prediction:
    Status: AT_RISK_LOST
    Missing Risk: 0.92
  Decision:
    Action: 立即巡检 (INSPECT)
    Priority: 1
  ✅ Correct

========================================
          决策命中率统计
========================================

Overall:
  Total: 8
  Correct: 8
  Accuracy: 100.0%
```

---

## 接入标准

满足以下条件才能接入主系统:

✅ 1. 决策命中率 ≥ 80%
✅ 2. 无明显误判类型
✅ 3. 告警数量可控（不会爆）

---

## 参数调优

可以动态调整阈值:
```cpp
DecisionEngine::instance().setMissingRiskThreshold(0.85);
DecisionEngine::instance().setActiveAssetThreshold(0.5);
```

可用的配置方法:
- `setMissingRiskThreshold()`
- `setAbnormalRiskThreshold()`
- `setInactiveRiskThreshold()`
- `setActiveAssetThreshold()`

---

## 下一步

### Phase 1: 验证 ✅
- [x] 创建决策沙盒
- [x] 优化决策引擎
- [x] 添加关键修复

### Phase 2: Shadow Mode
- [ ] 集成到现有系统
- [ ] 运行 3-7 天
- [ ] 收集验证数据

### Phase 3: 半自动
- [ ] 人工确认执行
- [ ] 持续调优

### Phase 4: 全自动
- [ ] 自动执行决策
- [ ] 自动生成任务
